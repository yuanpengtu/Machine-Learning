k均值聚类算法是一种迭代求解的聚类分析算法，其步骤是随机选取K个对象作为初始的聚类中心，
然后计算每个对象与各个种子聚类中心之间的距离，把每个对象分配给距离它最近的聚类中心。
聚类中心以及分配给它们的对象就代表一个聚类。每分配一个样本，聚类的聚类中心会根据聚类
中现有的对象被重新计算。这个过程将不断重复直到满足某个终止条件。终止条件可以是没有
（或最小数目）对象被重新分配给不同的聚类，没有（或最小数目）聚类中心再发生变化，误差平
方和局部最小。

这是我们做的聚类的动态演示，随着迭代次数的增加，可以看到边界不断变化，最后趋向于不变
然后在K-means算法中如何计算点群的中心也是一个问题，这里列举了三种计算的方式，
不同的公式对应逼近中心的方式不同，第一个图以星形的方式，第二个图以同心圆的方式，
第三个图以菱形的方式。
接下来是FCM算法的介绍，FCM算法是一种基于划分的聚类算法，它的思想就是使得被划分
到同一簇的对象之间相似度最大，而不同簇之间的相似度最小。模糊C均值算法是普通C均值算
法的改进，普通C均值算法对于数据的划分是硬性的，而FCM则是一种柔性的模糊划分而这种
模糊聚类的优点就是能够建立了样本对类别的不确定描述，更能客观的反应客观世界，
然后这是FCM算法的聚类步骤。
隶属度是FCM算法中一个重要的概念，实际上他在K-means中也有反映，只是值只能取0，1；
而在FCM算法中，隶属度可以取[0,1]，简单来说，就是里中心点越近，隶属度越大，这是隶属度的计算公式


然后接下来是Mean-shift算法的介绍

其实就是求解一个向量，使得圆心一直往数据集密度最大的方向移动。每次迭代的时候，都是找到圆里面点的平均位置作为新的圆心位置。
然后这是多维空间下Mean-shift的聚类步骤，实质就是沿着密度上升的方向寻找同属一个簇的数据点。
二维空间下圆形坐标推导

常用的核函数包含以下几个：高斯、拉普拉斯、线性，其中高斯和拉普拉斯核函数是经典的
鲁棒径向基核，对于数据中的噪音有着较好的抗干扰能力



三类聚类算法都是基于二维数组的数据点进行聚类，所以首先需要将图像转化为二维数组的形式
这是我们将图像读取为二维数组的处理步骤，这是计算欧式距离的公式
接下来是K-Means\FCM\Mean-shift具体在图像分割的效果对比